# AIEOS: AI Entity Object Specification

ZeroClaw supports the AIEOS standard for identity rotation and cross-agent authentication.

- **Issuer**: ZeroClaw Identity Provider (ZIP).
- **Format**: Signed JSON-LD.
- **Support**: OpenClaw (Legacy), AIEOS Core.
